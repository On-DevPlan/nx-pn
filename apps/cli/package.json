{
  "name": "@api-audit/cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": { "api-audit": "./bin/api-audit.mjs" },
  "files": ["bin", "lib"],
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@api-audit/host": "workspace:^"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "typescript": "5.6.3",
    "vitest": "2.1.5"
  }
}
