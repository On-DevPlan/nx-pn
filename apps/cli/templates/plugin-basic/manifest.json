{
  "schemaVersion": 1,
  "id": "{{id}}",
  "version": "0.1.0",
  "title": "{{title}}",
  "halves": {
    "host": { "entry": "host.js" },
    "browser": {
      "entry": "browser.js",
      "pages": [{ "path": "{{path}}", "title": "{{title}}" }]
    }
  }
}
